{{#each blogs as |blog| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{blog.blog_title}} {{blog.user_id}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{blog.blog_content}}</h3>
  </div>
</div>
{{/each}}
