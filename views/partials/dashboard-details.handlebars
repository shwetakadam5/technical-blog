{{ log this}}
{{!-- {{#each blogData }} --}}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2> {{blog_title}}  {{bloguser.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{blog_content}}</h3>
  </div>
</div>
{{!-- {{/each}} --}}